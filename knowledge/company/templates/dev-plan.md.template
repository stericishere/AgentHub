# 開發計畫書: {功能/Sprint 名稱}

> **撰寫者**: {L1 / Tech Lead}
> **日期**: {日期}
> **專案**: {專案名稱}
> **Sprint 提案書**: {連結}
> **狀態**: G0 通過，執行中

---

> 本文件在 G0 通過後由 L1 撰寫，依據提案書中勾選的步驟展開技術細節。

## 1. 需求摘要

{簡述要做什麼、為什麼要做，引用提案書}

### 確認的流程

{從提案書帶入，例：需求 → 設計 → 實作 → G2 → 測試 → G3 → 文件 → G4}

---

## 2. 技術方案

### 選定方案

{描述選定的技術實作方式}

### 替代方案比較（如適用）

| 方案 | 優點 | 缺點 | 結論 |
|------|------|------|------|
| A: {方案名} | | | ✅ 選定 |
| B: {方案名} | | | ❌ 排除 |

---

## 3. UI 圖稿（僅提案書勾選「UI 圖稿」時填寫）

| 頁面/元件 | Mockup 檔案 | 說明 |
|----------|------------|------|
| | `mockups/{name}.html` | |

### 圖稿驗收標準

- [ ] 所有新增/修改頁面皆有對應 HTML mockup
- [ ] 可在瀏覽器直接開啟預覽（不需 build）
- [ ] 繁體中文化完成
- [ ] 暗色/亮色主題皆可檢視（如適用）

---

## 4. 檔案變更清單

### 新增

| 檔案 | 用途 |
|------|------|
| | |

### 修改

| 檔案 | 變更內容 |
|------|---------|
| | |

### 刪除

| 檔案 | 原因 |
|------|------|
| | |

---

## 5. 介面設計（如適用）

### IPC / API 新增

| 通道/端點 | 方向 | 參數 | 回傳 |
|----------|------|------|------|
| | | | |

### 型別定義

```typescript
// 新增或修改的型別
```

---

## 6. 任務定義與分配

> L1 在此定義所有任務。老闆透過 PM 產生任務指令後手動建立，轉達給負責 Agent。

### 任務清單

| # | 任務名稱 | 說明 | 負責 Agent | 依賴 | 對應步驟 | 驗收標準 |
|---|---------|------|-----------|------|---------|---------|
| T1 | {任務名} | {具體要做什麼} | {agent-id} | 無 | 實作 | {完成條件} |
| T2 | {任務名} | {具體要做什麼} | {agent-id} | T1 | 實作 | {完成條件} |
| T3 | {任務名} | {具體要做什麼} | {agent-id} | T2 | 測試 | {完成條件} |

### 任務指令（DelegationCommand 格式）

> 老闆手動建立任務時，依此格式產生指令轉達給 Agent。格式對齊系統的 `DelegationCommand` 型別。

```json
{
  "targetAgent": "{agent-id}",
  "task": "{T# — 具體任務描述，含範圍和完成條件}",
  "priority": "medium",
  "context": "{背景資訊：依賴的前置任務、注意事項、禁止事項}"
}
```

#### 範例

```json
{
  "targetAgent": "fe-dev-01",
  "task": "T2 — SessionsView 新增分組切換下拉選單，支援 none/project/department 三種模式，分組偏好存 localStorage",
  "priority": "high",
  "context": "依賴 T1（sessions store groupedSessions computed）。不動 SessionCard 元件，只改 SessionsView。"
}
```

### 共用檔案（需協調）

| 檔案 | 涉及任務 | 風險等級 |
|------|---------|---------|
| | T#, T# | 高/中/低 |

---

## 7. 測試計畫（僅提案書勾選「測試」時填寫）

### 單元測試

| 測試檔案 | 測試案例 |
|---------|---------|
| | |

### E2E 測試

| 測試檔案 | 測試案例 |
|---------|---------|
| | |

---

## 8. 風險與緩解

| 風險 | 影響 | 緩解措施 |
|------|------|---------|
| | | |

---

## 9. 文件更新（僅提案書勾選「文件」時填寫）

完成後需同步更新的文件：

- [ ] CLAUDE.md
- [ ] {其他相關文件}

---

## 10. 任務與審核紀錄（備查）

> 每個任務完成後記錄結果，每次 Review/Gate 通過後記錄決策。本區作為 Sprint 完整稽核軌跡。

### 任務完成紀錄

| 任務 | 完成日期 | 結果 | 備註 |
|------|---------|------|------|
| T1 | | ✅ 完成 / 🔧 需修正 | |

### Review 紀錄

| Review 步驟 | 日期 | 結果 | Review 文件連結 |
|------------|------|------|---------------|
| {步驟名} | | ✅ 通過 → 提交 Gate | `docs/reviews/{sprint}-{step}.md` |

### Gate 紀錄

| Gate | 日期 | 決策 | 審核意見 |
|------|------|------|---------|
| G0 | {日期} | ✅ 通過 | {提案書連結} |
| {Gx} | | | |

---

**確認**: [ ] L1 確認 / [ ] Tech Lead 確認
